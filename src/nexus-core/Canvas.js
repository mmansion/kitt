define(function () {

   /* CANVAS CLASS
   --------------------------------------------------- */

  var Canvas = function() {
    var self = this;
  };

  /* CANVAS PROTOTYPE
   --------------------------------------------------- */

  Canvas.prototype = {
    
  
  };

   /* PRIVATE FUNCTIONS
   --------------------------------------------------- */

   var getTimeNow = function () {
      return new Date().getTime();
   };
  
  return new Canvas();
});